'PUMLAMR
'PUMLAPARENT: tempSys
@startuml
!include ./../../../pumla_macros.puml

!if ($PUMVarShowBody)
'add block definition with internals here, e.g.:
component "Temperature Sensor B (dC)" <<block>> as tempSensorBdC {

    ' the elements put here should have no or
    ' no other parent

    ' this automatically puts the ShowBodyInternals global variable
    ' check around.
    PUMLAPutInternalElementWithRel(tempSensorBdC, tempSensorB, "..>", "use")
    PUMLAPutInternalElementWithRel(tempSensorBdC, tempConverter, "..>", "use")

    ' if you do not need the relation you could also do it with this:
    'PUMLAPutInternalElement(tempSensorB)
}
!endif
' necessary to call when "PUMLAPutInternalElementWithRel" has been used
' inside the re-usable element definition
PUMLAFinalizeElement()

' define an interface
PUMLAPutInterface("temp_DC", temp_dC_tempSensorBdC)

' define the connection between interface and element
PUMLAPutConnection(temp_dC_tempSensorBdC, tempSensorBdC, "<--")
' define connection between the internal elements interface and
' this element
PUMLAPutConnection(temp_dC_tempConverter, temp_dC_tempSensorBdC, "--")
' define connection between both internal elements (their interfaces)
PUMLAPutConnection(temp_dF_tempSensorB, temp_dF_tempConverter, "--")

!if ($PUMVarShowDescr)
note bottom of tempSensorBdC
    Temperature Sensor of company B
    combined with the converter to
    provide the temperature in degree
    Celsius.
end note
!endif

@enduml